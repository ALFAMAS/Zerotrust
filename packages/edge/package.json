{
  "name": "@zeroauth/edge",
  "version": "0.1.0",
  "description": "Cloudflare Workers / Bun Edge compatible ZeroAuth distribution",
  "module": "src/worker.ts",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240314.0",
    "wrangler": "^3.40.0",
    "typescript": "^5.3.3"
  }
}

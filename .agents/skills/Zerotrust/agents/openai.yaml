interface:
  display_name: "Zerotrust"
  short_description: "Repo-specific patterns and workflows for Zerotrust"
  default_prompt: "Use the Zerotrust repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
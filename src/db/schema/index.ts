/**
 * Drizzle schema barrel — domain modules under this directory (DI-1).
 * `tables.ts` re-exports identity, organizations, billing, webhooks, …
 */

export * from "./tables";
export * from "./types";

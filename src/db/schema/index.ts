/**
 * Drizzle schema barrel — domain splits land under this directory (DI-1).
 * `tables.ts` holds the full model today; `types.ts` holds shared interfaces.
 */
export * from "./types";
export * from "./tables";
